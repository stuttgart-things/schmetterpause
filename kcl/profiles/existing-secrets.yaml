# The same application where something else already puts the two Secrets in
# place — sealed secrets, a manual kubectl, a different operator.
#
# Nothing changes for the Deployment: it reads <name>-app and <name>-db by name
# either way. Only the ExternalSecrets stop being rendered.
kcl_options:
  - key: config.namespace
    value: schmetterpause
  - key: config.image
    value: ghcr.io/stuttgart-things/schmetterpause:latest
  - key: config.secretsMode
    value: existing
