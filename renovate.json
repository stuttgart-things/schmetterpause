{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:recommended"
  ],
  "customManagers": [
    {
      "customType": "regex",
      "description": "Container images pinned as Go constants in the Dagger pipeline module. Renovate's docker manager only sees Dockerfiles and compose files, so without this the pipeline images drift behind the ones it does see.",
      "managerFilePatterns": [
        "/^dagger/main\\.go$/"
      ],
      "matchStrings": [
        "Image\\s*=\\s*\"(?<depName>[^\":\\s]+):(?<currentValue>[^\"\\s]+)\""
      ],
      "datasourceTemplate": "docker"
    },
    {
      "customType": "regex",
      "description": "Runtime base image. Since ADR-0009 it lives in .ko.yaml instead of a Dockerfile, where Renovate's docker manager would have found it by itself.",
      "managerFilePatterns": [
        "/^\\.ko\\.yaml$/"
      ],
      "matchStrings": [
        "defaultBaseImage:\\s*(?<depName>[^:\\s]+):(?<currentValue>\\S+)"
      ],
      "datasourceTemplate": "docker"
    },
    {
      "customType": "regex",
      "description": "ko is pinned twice: once for the local build in the Taskfile, once for the pipeline in the Dagger module. They build the same image and have to move together, so one rule covers both files.",
      "managerFilePatterns": [
        "/^Taskfile\\.yml$/",
        "/^dagger/main\\.go$/"
      ],
      "matchStrings": [
        "KO_VERSION:\\s*(?<currentValue>v\\S+)",
        "koVersion\\s*=\\s*\"(?<currentValue>v[^\"\\s]+)\""
      ],
      "depNameTemplate": "ghcr.io/ko-build/ko",
      "datasourceTemplate": "docker"
    }
  ],
  "packageRules": [
    {
      "description": "A major Postgres bump refuses to start on any data directory an older major version created, so every existing development volume dies with it. Requiring approval on the dashboard turns that from a surprise into a decision — it does not block the upgrade, it just stops it arriving unannounced.",
      "matchDatasources": [
        "docker"
      ],
      "matchPackageNames": [
        "postgres"
      ],
      "matchUpdateTypes": [
        "major"
      ],
      "dependencyDashboardApproval": true
    }
  ]
}
